import { Card, CardHeader, CardBody, CardFoote, Text } from '@chakra-ui/react';

function Care({ care }) {
  return (
    <Card>
      <CardBody>
        <Text>Pagina de Cares</Text>
      </CardBody>
    </Card>
  );
}

export default Care;
