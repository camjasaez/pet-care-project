import { Card, CardHeader, CardBody, CardFoote, Text } from '@chakra-ui/react';

function Pet({ pet }) {
  return (
    <Card>
      <CardBody>
        <Text>Pagina de Pet</Text>
      </CardBody>
    </Card>
  );
}

export default Pet;
